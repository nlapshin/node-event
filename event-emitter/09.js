const fs = require('fs')

// loop
while(true) {
  // pool
  queue.run() // Есть ли в очереди новые задачи? Если да, то выполнить.
}

fs.read // асинхронный - делегирует выполнение OS и ждет фоном
fs.readSync // синхронные - делегирует выполнение OS и ждет не фоном

// libuv - C/C++ библиотека, которая занимается созданием event loop.
